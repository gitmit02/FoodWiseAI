# Not required for the default setup — the client is hardcoded to talk to
# http://localhost:5000 in src/api.js. This file is kept only in case you
# want to later move the API base URL into an environment variable.
VITE_API_BASE_URL=http://localhost:5000
